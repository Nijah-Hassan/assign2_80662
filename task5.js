var numBer = 8;
var guessNumber = prompt("Guess a Number From 1 to 10?");
var correctAns = parseInt(numBer);

if (guessNumber == numBer)
    {alert("Bingo! Correct answer");
}
    else if (guessNumber == numBer + 1)
    {alert("Close enough to the correct answer");
}
 